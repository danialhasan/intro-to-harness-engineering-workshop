# Task: plan a safe service release

Repair `src/release-plan.ts` so it produces a valid service release order.

Read `docs/release-graph-contract.md` and the existing types before editing.
Preserve the exported function and type names. Do not edit `docs/`, `test/`,
`verifier/`, `reference/`, package files, or TypeScript configuration.

Run the participant-visible tests and any focused checks you need. The fixed
evaluator is the acceptance authority.
