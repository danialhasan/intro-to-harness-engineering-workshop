# Task: stop only after release evidence is complete

Repair `src/release-decision.ts` so it creates a release marker only when the
public release contract is satisfied.

Read `docs/release-contract.md` and the existing types before editing. Preserve
the exported function and type names. Do not edit `docs/`, `test/`, `verifier/`,
`reference/`, package files, or TypeScript configuration.

Run the participant-visible tests and any focused checks you need. The fixed
evaluator is the acceptance authority.
