# Context contract Taskset

- Task ID: `context-contract/v1`
- Failure pattern: missing context, requirements, or codebase grounding.
- Synthetic scenario: an import planner uses plausible defaults instead of the
  repository's public service contract.
- Fixed evidence: route, tenant header, batch limit, order preservation, and
  blank-tenant behavior.

This Taskset contains no participant data and does not reproduce a survey
response. See the parent [Taskset library](../README.md) for validation and
privacy rules.
