# Workshop

This folder will contain the runnable participant materials.

Planned contents:

- preflight instructions
- one fixed engineering task
- a real coding-agent runner
- the participant-editable harness configuration
- a deterministic evaluator
- public-safe example trajectories and comparison output

Participants will change the harness around an existing agent loop. They will not build a new agent loop during the session.
